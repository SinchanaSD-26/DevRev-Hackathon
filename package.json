// Manages Node.js dependencies and scripts.

SNAP IN :-
{
  "name": "customer-frustration-alerting-system",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@slack/web-api": "^6.8.0",
    "nodemailer": "^6.9.3",
    "sentiment": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}


// Expected Output:

// After running npm install, all dependencies (like @slack/web-api, nodemailer, sentiment) will be installed.
// npm run build compiles TypeScript files.
// npm start runs the compiled index.js.
